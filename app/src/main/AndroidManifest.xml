<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beitshean.mylaundry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserLaundryWeightOrderActivity"></activity>
        <activity android:name=".UserHomeWeightOrderActivity" />
        <activity android:name=".ManagerExistingOrdersActivity" />
        <activity android:name=".UserExistingOrdersActivity" />
        <activity android:name=".UserNewOrderTypesActivity" />
        <activity android:name=".ManagerHomePageActivity" />
        <activity android:name=".UserHomePageActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>